seed: 42
device: cuda:0
stage: deterministic
data:
  format: diffcast_h5
  root: /test1/wzq/Weather/PhyDNet/data/sevir/sevir_vil_only_25frames_384_diffcast.h5
  split: train
  max_samples: null
  input_frames: 5
  output_frames: 20
  window_start_index: 0
  model_resolution: 128
  spatial_preprocess: diffcast_bilinear
  batch_size: 8
  num_workers: 4
model:
  base_channels: 64
  diffusion_steps: 100
  freeze_deterministic: false
  deterministic_checkpoint: null
  deterministic:
    name: sdir_official
    params:
      patch_size: 8
      hidden_size: 512
      num_heads: 4
      depth: 8
      frequency_stride: 16
      curriculum_alpha: 1.0
      curriculum_beta: 3.0
      pcpsd_weight: 0.01
      model_resolution: 128
optimization:
  learning_rate: 0.0003
  weight_decay: 0.0
  max_epochs: 50
  log_every: 10
  precision: bf16
validation:
  enabled: true
  split: valid
  every_epochs: 10
physics:
  enabled: false
  robust_scale: 0.05
  tolerance: 0.1
  gamma_nadv: 1.0
  pool_sizes: [8, 16, 32]
  alpha_mass: 0.25
  lambda_train: 0.0
artifacts:
  directory: artifacts/experiments/phyrd_sdir_diffcast_5to20_ddp8_seed42
