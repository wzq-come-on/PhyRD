# Offline deterministic trend caches

Server-only NumPy memmap caches generated by `scripts/precompute_trends.py`
live under this directory. They are deliberately excluded from Git because
they can be tens of gigabytes and are tied to a specific deterministic
checkpoint and data protocol.
